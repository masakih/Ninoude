
module Curl [system] {
    link "curl"
    umbrella header "/usr/include/curl/curl.h"
    export *
    module * { export * }
}

module CurlWrapper [system] {
    link "curl"
    header "CurlWrapper.h"
    export *
}
